$(document).ready(function() {
  $("form#blanks").submit(function(event) {
    var betty = ("input#gender").val();
      if (gender === "female" && age >= 35) {
      (#output5(.show))
      }

    event.preventDefault();
  });
});
